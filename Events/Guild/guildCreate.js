module.exports = {
    name: "guildCreate",
    /**
     * 
     * @param {CommandInteraction} interaction 
     * @param {Client} client 
     */
    async execute(guild) {
        process.exit();
    }
}